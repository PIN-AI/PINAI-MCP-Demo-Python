[project]
name = "pinai-mcp-walletdata"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
    "starlette>=0.46.1",
    "uvicorn>=0.34.0",
]
